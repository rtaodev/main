//  ███████╗ █████╗ ███████╗    ██╗  ██╗
//  ██╔════╝██╔══██╗██╔════╝    ╚██╗██╔╝
//  █████╗  ███████║███████╗     ╚███╔╝ 
//  ██╔══╝  ██╔══██║╚════██║     ██╔██╗ 
//  ███████╗██║  ██║███████║    ██╔╝ ██╗
//  ╚══════╝╚═╝  ╚═╝╚══════╝    ╚═╝  ╚═╝
// EAS-X Advanced Bypass System
// by EAS-X Team - Based on F.E.A.R. by Woozie
// Website: https://eas-x.com
// F.E.A.R. Original: https://trw.lat/install/userscript/u.raw.js
// "Advanced Link Bypassing Technology"
// Thats an fork of F.E.A.R. Userscript That uses EASx API.

// ==UserScript==
// @name         EASx - F.E.A.R. V2.1 (Main)
// @namespace    https://eas-x.com
// @version      2.1
// @description  Advanced AdLink bypass powered by EAS-X API (Based on F.E.A.R. by Woozie) - Main userscript for all sites except work.ink
// @author       EAS-X Team & Woozie (Original F.E.A.R.)
// @license      MIT
// @match        *://linkvertise.com/*
// @match        *://loot-link.com/*
// @match        *://loot-links.com/*
// @match        *://lootdest.com/*
// @match        *://lootdest.org/*
// @match        *://lootlinks.co/*
// @match        *://lootlinks.com/*
// @match        *://daughablelea.com/*
// @match        *://tonordersitye.com/*
// @match        *://cuty.io/*
// @match        *://www.cuty.io/*
// @match        *://cety.app/*
// @match        *://www.cety.app/*
// @match        *://lockr.so/*
// @match        *://mega-guy.com/*
// @match        *://best-links.org/*
// @match        *://megaspremium.com/*
// @match        *://direct-links.net/*
// @match        *://risquemega.com/*
// @match        *://onlyfriends.club/*
// @match        *://onepiecered.co/*
// @match        *://multileaks.com/*
// @match        *://luvsquad-links.com/*
// @match        *://luvsquad-links.cmo/*
// @match        *://free-leaks.com/*
// @match        *://goldmega.online/*
// @match        *://realiukzemydre.com/*
// @match        *://kmendation.com/*
// @match        *://onlyshare.info/*
// @match        *://leakplugs.com/*
// @match        *://nswfbox.com/*
// @match        *://eofmukindwo.com/*
// @match        *://op-packs.com/*
// @match        *://thedetective.online/*
// @match        *://forlinkmeg.com/*
// @match        *://thegoatpack.org/*
// @match        *://of-leaks.xyz/*
// @match        *://links.spacebin.in/*
// @match        *://supermeg.com/*
// @match        *://onlyfanessereloaded.com/*
// @match        *://fanzleaks.com/*
// @match        *://cemendemons.com/*
// @match        *://premiummegaz.com/*
// @match        *://thepremium.online/*
// @match        *://pasteebins.com/*
// @match        *://baddiezcentral.com/*
// @match        *://megazone.website/*
// @match        *://leak-pragmatic.com/*
// @match        *://megadumpz.com/*
// @match        *://thhaven.net/*
// @match        *://meg4fans.com/*
// @match        *://depravityweb.co/*
// @match        *://discordlink.cc/*
// @match        *://megashub.co/*
// @match        *://streamergirls.org/*
// @match        *://your-leaks.com/*
// @match        *://onlylinksmegas.xyz/*
// @match        *://pypy.spacebin.in/*
// @match        *://pypy.in/*
// @match        *://beast.net.in/*
// @match        *://hook.beast.net.in/*
// @match        *://all-fans.online/*
// @match        *://fansmega.com/*
// @match        *://worldpacks.co/*
// @match        *://night-hub.online/*
// @match        *://dailyofleaks.com/*
// @match        *://pkofs.com/*
// @match        *://offree90.com/*
// @match        *://megadropz.com/*
// @match        *://onlyfunlink.com/*
// @match        *://direct-links.org/*
// @match        *://leaks4you.com/*
// @match        *://onlylinksmegas.com/*
// @match        *://direct-link.net/*
// @match        *://downbadleaks.com/*
// @match        *://onlyfanscloud.com/*
// @match        *://missleakz.com/*
// @match        *://leakszone.online/*
// @match        *://links-loot.com/*
// @match        *://holyfanslinks.com/*
// @match        *://utopianleaks.com/*
// @match        *://megavip.store/*
// @match        *://drlinker.com/*
// @match        *://baddiesheaven.com/*
// @match        *://of4lm-links.com/*
// @match        *://holedonly.store/*
// @match        *://free-content.pro/*
// @match        *://milky-center.com/*
// @match        *://megaofs.com/*
// @match        *://bleleadersto.com/*
// @match        *://link-target.org/*
// @match        *://mymegalinks.com/*
// @match        *://heroslut.com/*
// @match        *://vip-linknetwork.com/*
// @match        *://birdbiss.com/*
// @match        *://loot-labs.com/*
// @match        *://lootlabs.com/*
// @match        *://link-hub.net/*
// @match        *://locconnect.com/*
// @match        *://premiumstashdrop.com/*
// @match        *://lootdest.info/*
// @match        *://of-area.com/*
// @match        *://link-target.net/*
// @match        *://megalnk.com/*
// @match        *://lootlink.org/*
// @match        *://nsfwcherry.com/*
// @match        *://cherrypacks.online/*
// @match        *://hotstars-leaks.com/*
// @match        *://onlymega.co/*
// @match        *://hanimeturks.com/*
// @match        *://dailyleakz.com/*
// @match        *://content-hub.club/*
// @match        *://dailyadultmegas.com/*
// @match        *://oui-chu.com/*
// @match        *://megalinks.one/*
// @match        *://starleakz.com/*
// @match        *://babeslink.click/*
// @match        *://ofgirls3x.com/*
// @match        *://pnp-drops.me/*
// @match        *://nsfw-paradise.club/*
// @match        *://key-access.co/*
// @match        *://leaksmix.com/*
// @match        *://sensual-leaks.com/*
// @match        *://ofhub-leaks.com/*
// @match        *://onlyforfan.online/*
// @match        *://sweetjuice-mega.com/*
// @match        *://crip-hub.com/*
// @match        *://leaksfreeday.com/*
// @match        *://thotshaven.online/*
// @match        *://mega-leaked.com/*
// @match        *://beautifulgirls.social/*
// @match        *://tavernleaks.com/*
// @match        *://nudeleaksteenz.com/*
// @match        *://slutywet.com/*
// @match        *://leakutopia.site/*
// @match        *://secret-packs.com/*
// @match        *://attiktok22.com/*
// @match        *://pancakes-leaks.com/*
// @match        *://diamond-leaks.com/*
// @match        *://lewd-leaks.com/*
// @match        *://ftbleaks.net/*
// @match        *://thepytheaven.org/*
// @match        *://of-region.online/*
// @match        *://admiregirls-byme.com/*
// @match        *://mzehoney12.com/*
// @match        *://herplace.online/*
// @match        *://megaplugleaks.com/*
// @match        *://of-kingdom.com/*
// @match        *://onlyfansmegafolder.com/*
// @match        *://adfoc.us/*
// @match        *://boost.ink/*
// @match        *://mboost.me/*
// @match        *://bst.gg/*
// @match        *://booo.st/*
// @match        *://mendationforc.info/*
// @match        *://paster.so/*
// @match        *://paster.gg/*
// @match        *://rekonise.com/*
// @match        *://social-unlock.com/*
// @match        *://www.social-unlock.com/*
// @match        *://socialwolvez.com/*
// @match        *://sub2get.com/*
// @match        *://www.sub2get.com/*
// @match        *://sub2unlock.com/*
// @match        *://sub2unlock.io/*
// @match        *://sub2unlock.net/*
// @match        *://sub2unlock.online/*
// @match        *://sub2unlock.top/*
// @match        *://sub4unlock.pro/*
// @match        *://sub4unlock.com/*
// @match        *://sub4unlock.io/*
// @match        *://subfinal.com/*
// @match        *://unlocknow.net/*
// @match        *://v.gd/*
// @match        *://workink.net/*
// @match        *://r.work.ink/*
// @match        *://workink.one/*
// @match        *://workink.me/*
// @match        *://ytsubme.com/*
// @match        *://www.ytsubme.com/*
// @match        *://esohasl.net/*
// @match        *://rbscripts.net/*
// @match        *://link.rbscripts.net/*
// @match        *://usrnew.eas-x.com/bypass?url=*
// @match        *://usrnew.eas-x.com/workink?url=*
// @match        *://*.*.*/*?easxreferrer=*
// @match        *://usrnew.eas-x.com/?url=*
// @match        *://*.*.*/easxlol*
// @match        *://localhost:13075/*
// @match        *://localhost:3000/*
// @match        *://127.0.0.1:13075/*
// @match        *://127.0.0.1:3000/*
// @exclude      *://tria.ge/*
// @exclude      *://*.google.*/*
// @exclude      *://work.ink/*
// @exclude      *://www.work.ink/*

// @grant        GM_xmlhttpRequest
// @grant        GM_getResourceText
// @grant        unsafeWindow
// @grant        GM_addStyle
// @require      https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js
// @resource     NOTYF_CSS https://cdn.jsdelivr.net/npm/notyf@3/notyf.min.css
// @require      https://cdn.jsdelivr.net/npm/notyf@3/notyf.min.js
// @downloadURL  https://bypass.eas-x.com/eas-x-fear.js
// @updateURL    https://bypass.eas-x.com/eas-x-fear.js
// @homepageURL  https://bypass.eas-x.com
// @supportURL   https://bypass.eas-x.com/support
// @run-at       document-body
// ==/UserScript==

(function () {
  'use strict';
  if (window.top !== window.self) {return}
  
  // ================================
  // EARLY URL CAPTURE
  // ================================
  
  // Capture the original URL as early as possible before any modifications
  (function captureOriginalUrl() {
    if (!unsafeWindow.easxEarlyCapture) {
      const currentUrl = location.href;
      
      // Skip if this is clearly a resource or processed URL
      if (!currentUrl.includes('favicon.ico') && !currentUrl.includes('.css') && !currentUrl.includes('.js')) {
        unsafeWindow.easxEarlyCapture = currentUrl;
        
        // Store in sessionStorage immediately
        try {
          sessionStorage.setItem('easx_early_capture', currentUrl);
          console.log('[EAS-X] Early capture of URL:', currentUrl);
        } catch (e) {}
      }
    }
  })();
  
  if (location.hostname.includes("loot-link") || location.hostname.includes("lootdest") || location.hostname.includes("lootlink")){setInterval(()=>{document.querySelector(".fc-monetization-dialog-container")?.remove()},2500)}
  if (location.href.startsWith("http://bypass.eas-x.com/workink?url=") || 
      location.href.startsWith("https://bypass.eas-x.com/workink?url=") ||
      location.href.startsWith("http://localhost:13075/workink?url=") ||
      location.href.startsWith("http://localhost:3000/workink?url=")){
    unsafeWindow.EASX_Running=true;
    return;
  }
  // Environment detection
  const isLocalhost = location.hostname === 'localhost' || location.hostname === '127.0.0.1';
  const isLocalPort = location.port === '13075' || location.port === '3000';
  const isDevelopment = isLocalhost && isLocalPort;
  
  // Global retry function for website retry buttons
  unsafeWindow.easxRetry = function() {
    console.log('[EAS-X] Retry function called from website');
    
    // Get the original URL for retry
    let retryUrl = null;
    
    // Special handling if we're on a bypass page - extract the original URL from params
    if (location.pathname === '/bypass' && location.search.includes('url=')) {
      const urlParam = new URLSearchParams(location.search).get('url');
      if (urlParam) {
        retryUrl = decodeURIComponent(urlParam);
        console.log('[EAS-X] Extracted URL from bypass page params:', retryUrl);
      }
    }
    
    // Priority 1: Use early capture if available (only if not a bypass URL)
    if (!retryUrl && unsafeWindow.easxEarlyCapture && 
        !unsafeWindow.easxEarlyCapture.includes('favicon.ico') &&
        !unsafeWindow.easxEarlyCapture.includes('/bypass?url=')) {
      retryUrl = unsafeWindow.easxEarlyCapture;
    }
    
    // Priority 2: Try sessionStorage early capture (only if not a bypass URL)
    if (!retryUrl) {
      try {
        const earlyCapture = sessionStorage.getItem('easx_early_capture');
        if (earlyCapture && !earlyCapture.includes('favicon.ico') && 
            !earlyCapture.includes('/bypass?url=')) {
          retryUrl = earlyCapture;
        }
      } catch (e) {}
    }
    
    // Priority 3: SessionStorage original URL (only if not a bypass URL)
    if (!retryUrl) {
      try {
        const storedOriginal = sessionStorage.getItem('easx_original_url');
        if (storedOriginal && !storedOriginal.includes('favicon.ico') &&
            !storedOriginal.includes('/bypass?url=')) {
          retryUrl = storedOriginal;
        }
      } catch (e) {}
    }
    
    // Priority 4: unsafeWindow stored URLs (only if not a bypass URL)
    if (!retryUrl) {
      if (unsafeWindow.easxOriginalUrl && !unsafeWindow.easxOriginalUrl.includes('favicon.ico') &&
          !unsafeWindow.easxOriginalUrl.includes('/bypass?url=')) {
        retryUrl = unsafeWindow.easxOriginalUrl;
      } else if (unsafeWindow.easxStoredOriginal && !unsafeWindow.easxStoredOriginal.includes('favicon.ico') &&
                 !unsafeWindow.easxStoredOriginal.includes('/bypass?url=')) {
        retryUrl = unsafeWindow.easxStoredOriginal;
      }
    }
    
    // Final fallback
    if (!retryUrl) {
      retryUrl = 'https://linkvertise.com/';
    }
    
    // Ensure we have a valid URL and clean it
    try {
      // Clean the URL - remove any double protocols
      retryUrl = retryUrl.replace(/^https:\/\/https:\/\//, 'https://');
      retryUrl = retryUrl.replace(/^http:\/\/https:\/\//, 'https://');
      retryUrl = retryUrl.replace(/^https:\/\/http:\/\//, 'http://');
      
      new URL(retryUrl);
    } catch (e) {
      retryUrl = 'https://linkvertise.com/';
    }
    
    console.log('[EAS-X] Global retry with URL:', retryUrl);
    
    // Restart the bypass process
    const protocol = isDevelopment ? 'http:' : 'https:';
    const CONFIG = {
      wait: unsafeWindow.easxSecureMode ? 25 : 1,
      apiBase: isDevelopment ? 'localhost:13075' : 'bypass.eas-x.com'
    };
    const newBypassUrl = `${protocol}//${CONFIG.apiBase}/eas.lol?url=${encodeURIComponent(retryUrl)}&wait=${CONFIG.wait}`;
    
    console.log('[EAS-X] Redirecting to:', newBypassUrl);
    window.location.href = newBypassUrl;
  };
  
  // Enhanced retry button detection
  function findRetryButton() {
    const selectors = [
      'button[onclick*="retry"]',
      'button[onclick*="Retry"]', 
      'button:contains("Try Again")',
      'button:contains("Retry")',
      'button:contains("Try again")',
      'button:contains("RETRY")',
      'button:contains("TRY AGAIN")',
      '#retry-btn',
      '.retry-btn',
      '.try-again',
      '[data-action="retry"]',
      'button[title*="retry"]',
      'button[title*="Try again"]'
    ];
    
    for (const selector of selectors) {
      const button = document.querySelector(selector);
      if (button) return button;
    }
    
    // Check for buttons with retry-related text
    const buttons = document.querySelectorAll('button, .btn, [role="button"]');
    for (const button of buttons) {
      const text = button.textContent?.toLowerCase() || '';
      const title = button.title?.toLowerCase() || '';
      const ariaLabel = button.getAttribute('aria-label')?.toLowerCase() || '';
      
      if (text.includes('retry') || text.includes('try again') || 
          title.includes('retry') || title.includes('try again') ||
          ariaLabel.includes('retry') || ariaLabel.includes('try again')) {
        return button;
      }
    }
    
    return null;
  }
  
  const CONFIG = {
    wait: 1, // Initial delay (in seconds) before starting the bypass process.
    site: isDevelopment ? 'localhost:13075/eas.lol?url=' : 'bypass.eas-x.com/eas.lol?url=', // EAS.LOL wait page
    timeout: 6000000, // Maximum time (in milliseconds) to wait for the bypass completion check.
    interval: 500, // Interval (in milliseconds) between checks for bypass completion.
    SecureMode: true,
    //^^ Enables SecureMode, which increases the wait time to 30 seconds and adds a manual click-button prompt to proceed
    //^^ Enhancing stealth and reducing detectability of the bypass by up to 100%.
    apiBase: isDevelopment ? 'http://localhost:13075' : 'https://bypass.eas-x.com', // Auto-detect environment
    bypassPage: isDevelopment ? 'http://localhost:13075/bypass?url=' : 'https://bypass.eas-x.com/bypass?url=' // Direct bypass page
  };

  // Adjust wait time if SecureMode is true
  if (CONFIG.SecureMode) {
    CONFIG.wait = 25;
    unsafeWindow.easxSecureMode = true; // Store for global retry function
  }

  // Global click listener for retry buttons
  document.addEventListener('click', function(e) {
    const target = e.target;
    const text = target.textContent?.toLowerCase() || '';
    const title = target.title?.toLowerCase() || '';
    const onclick = target.getAttribute('onclick') || '';
    
    // Check if this looks like a retry button
    if ((text.includes('retry') || text.includes('try again') || 
         title.includes('retry') || title.includes('try again') ||
         onclick.includes('retry')) && 
        (target.tagName === 'BUTTON' || target.classList.contains('btn') || target.getAttribute('role') === 'button')) {
      
      console.log('[EAS-X] Retry button click detected');
      
      // Check if we're on a bypass page that failed
      if (document.querySelector("#bypass-title")?.textContent === "BYPASS FAILED!" ||
          document.body.innerHTML.includes('bypass failed') ||
          document.body.innerHTML.includes('Bypass failed') ||
          document.body.innerHTML.includes('BYPASS FAILED')) {
        
        e.preventDefault();
        e.stopPropagation();
        
        console.log('[EAS-X] Intercepting retry button for EAS-X retry');
        unsafeWindow.easxRetry();
      }
    }
  }, true); // Use capture phase to ensure we catch it first

  const notyfCss = GM_getResourceText("NOTYF_CSS");
  GM_addStyle(notyfCss);
  const notyf = new Notyf({ duration: 5000 });
  notyf.information = function(message) {
    this.success({
      message: message,
      duration: 4000,
      dismissible: true,
      background: '#1E88E5',
      icon: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="white" viewBox="0 0 16 16"><path d="M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16zm.93-9.412l-1 4.705c-.07.34.029.533.304.533.194 0 .487-.07.686-.246l-.088.416c-.287.346-.92.598-1.465.598-.703 0-1.002-.422-.808-1.319l.738-3.468c.064-.302.156-.506.258-.678l-.803-.303c-.21.105-.402.26-.523.465l-.728 3.424c-.07.34-.029.533-.304.533-.194 0-.487.07-.686.246l.088-.416c.287-.346.92-.598 1.465-.598.703 0 1.002.422.808 1.319l-.738 3.468c-.064.302-.156.506-.258.678l.803.303c.21-.105.402-.26.523-.465l.728-3.424c.07-.34.029-.533.304-.533.194 0 .487.07.686.246l-.088.416c-.287.346-.92.598-1.465.598-.703 0-1.002-.422-.808-1.319l.738-3.468c-.064.302-.156.506-.258-.678l-.803-.303c-.21.105-.402.26-.523.465z"/></svg>'
    });
  };

  const Log = {
    i: (msg) => console.log(`[EAS-X F.E.A.R.] ${msg}`),
    e: (msg) => console.error(`[EAS-X F.E.A.R. ERROR] ${msg}`),
    w: (msg) => console.warn(`[EAS-X F.E.A.R. WARN] ${msg}`),
  };

  async function sleep(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
  }

  async function findHref() {
    const start = Date.now();
    while (Date.now() - start < CONFIG.timeout) {
      await sleep(CONFIG.interval);
      try {
        if (document.querySelector("#bypass-title")?.textContent === "BYPASS DONE" && unsafeWindow.easxSuccess) {
          return unsafeWindow.easxResult;
        } else if (document.querySelector("#bypass-title")?.textContent === "BYPASS FAILED!") {
          return false;
        }
      } catch (err) {
        console.log(`Find href error: ${err}`);
      }
    }
    return false;
  }

  function isBot() {
    const t = document.title;
    return t.includes('Just a moment') || t.includes('Just a second') || document.body.innerHTML.includes('Are you human?');
  }

  function isIndexPage() {
    const url = location.href.toLowerCase();
    const pathname = location.pathname.toLowerCase();
    
    // Check if it's a root/index page
    if (pathname === '/' || pathname === '' || pathname === '/index' || pathname === '/index.html') {
      return true;
    }
    
    // Check for common index page patterns
    const indexPatterns = [
      /\/index\.html?$/,
      /\/home\.html?$/,
      /\/main\.html?$/,
      /\/$/, // Just domain with trailing slash
      /^https?:\/\/[^\/]+\/?$/ // Just domain with optional trailing slash
    ];
    
    for (const pattern of indexPatterns) {
      if (pattern.test(url)) {
        return true;
      }
    }
    
    // Check if URL has no path parameters (likely index)
    if (!url.includes('?') && !url.includes('#') && pathname.split('/').length <= 2) {
      return true;
    }
    
    return false;
  }

  async function main() {
    if (isBot()) {
      notyf.information('Complete the bot challenge to proceed');
      return;
    }

    // Don't show popup on index pages - nothing to bypass
    if (isIndexPage()) {
      Log.i('Index page detected, skipping bypass popup');
      return;
    }

    const p = new URLSearchParams(window.location.search);
    const ref = p.get('easxreferrer');
    if (ref) {
      // Store the original URL for retry functionality (smart extraction)
      if (!unsafeWindow.easxOriginalUrl) {
        let originalUrl = null;
        
        // Priority 1: Use early capture if available
        if (unsafeWindow.easxEarlyCapture) {
          originalUrl = unsafeWindow.easxEarlyCapture;
        }
        
        // Priority 2: Try sessionStorage early capture
        if (!originalUrl) {
          try {
            originalUrl = sessionStorage.getItem('easx_early_capture');
          } catch (e) {}
        }
        
        // Priority 3: Current URL (cleaned)
        if (!originalUrl) {
          originalUrl = location.href;
          
          // Remove any hash parameters or processing markers
          originalUrl = originalUrl.split('#')[0];
          originalUrl = originalUrl.split('?hash=')[0];
          originalUrl = originalUrl.split('&hash=')[0];
          
          // Check if this looks like a bypass URL pattern and extract the real original
          if (originalUrl.includes('bypass.vip') || originalUrl.includes('rip.linkvertise')) {
            // Try to extract from referrer or URL params
            const urlParams = new URLSearchParams(location.search);
            if (urlParams.get('url')) {
              originalUrl = decodeURIComponent(urlParams.get('url'));
            } else if (document.referrer && !document.referrer.includes('bypass')) {
              originalUrl = document.referrer;
            }
          }
        }
        
        // Avoid favicon.ico and other resource URLs
        if (!originalUrl || originalUrl.includes('favicon.ico') || originalUrl.includes('.css') || originalUrl.includes('.js')) {
          // Fall back to the hostname
          originalUrl = `https://${location.hostname}/`;
        }
        
        unsafeWindow.easxOriginalUrl = originalUrl;
        
        // Store in sessionStorage as backup
        try {
          sessionStorage.setItem('easx_original_url', originalUrl);
        } catch (e) {
          console.log('[EAS-X] Could not store in sessionStorage:', e);
        }
        
        console.log('[EAS-X] Stored original URL for retry:', originalUrl);
      }
      
      history.replaceState(null, null, '/' + ([1e7]+-1e3+-4e3+-8e3+-1e11).replace(/[018]/g, c => (c ^ crypto.getRandomValues(new Uint8Array(1))[0] & 15 >> c/4).toString(16)))
      setInterval(() => history.replaceState(null, null, '/' + ([1e7]+-1e3+-4e3+-8e3+-1e11)
        .replace(/[018]/g, c => (c ^ crypto.getRandomValues(new Uint8Array(1))[0] & 15 >> c/4).toString(16))), 1000);

      if (CONFIG.SecureMode) {
        unsafeWindow.dest = decodeURIComponent(ref);
        document.documentElement.innerHTML = `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EAS-X F.E.A.R. - Ready to Proceed</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #1E88E5;
            --primary-dark: #1565C0;
            --secondary: #2196F3;
            --accent: #FF5722;
            --background: #0F1419;
            --surface: #1A1F2E;
            --surface-light: #252B3F;
            --text: #E0E6ED;
            --text-secondary: #A0A6B0;
            --success: #4CAF50;
            --warning: #FF9800;
            --error: #F44336;
            --border: #2D3748;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--background);
            color: var(--text);
            line-height: 1.6;
            overflow-x: hidden;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, 
                var(--background) 0%, 
                #1a1f2e 25%, 
                var(--background) 50%, 
                #252b3f 75%, 
                var(--background) 100%);
            background-size: 400% 400%;
            animation: gradientShift 15s ease infinite;
            z-index: -1;
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .container {
            background: var(--surface);
            padding: 3rem 2.5rem;
            border-radius: 20px;
            border: 1px solid var(--border);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
            max-width: 600px;
            width: 90%;
            text-align: center;
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }

        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, var(--primary), transparent);
        }

        .logo {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .title {
            font-size: 1.75rem;
            color: var(--text);
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .subtitle {
            color: var(--text-secondary);
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--success);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-weight: 600;
            margin-bottom: 2rem;
            font-size: 0.9rem;
        }

        .countdown {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--warning);
            margin-bottom: 1rem;
        }

        .next-btn {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            border: none;
            padding: 1rem 2rem;
            border-radius: 12px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(30, 136, 229, 0.3);
            font-family: 'Inter', sans-serif;
        }

        .next-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(30, 136, 229, 0.4);
        }

        .next-btn:disabled {
            background: var(--surface-light);
            color: var(--text-secondary);
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        .warning-text {
            background: var(--warning);
            color: white;
            padding: 1rem;
            border-radius: 10px;
            margin-bottom: 1.5rem;
            font-weight: 600;
            font-size: 0.95rem;
        }

        .footer {
            margin-top: 2rem;
            font-size: 0.8rem;
            color: var(--text-secondary);
            opacity: 0.8;
        }

        .footer a {
            color: var(--primary);
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .container {
                padding: 2rem 1.5rem;
                margin: 1rem;
            }
            
            .logo {
                font-size: 2rem;
            }
            
            .title {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="bg-animation"></div>
    
    <div class="container">
        <div class="logo">🚀 EAS-X F.E.A.R.</div>
        <div class="status-badge">
            ✅ Bypass Complete
        </div>
        <h1 class="title">Ready to Proceed</h1>
        <p class="subtitle">Your link has been successfully bypassed. Click the button below to access your destination.</p>
        
        <div id="countdown" class="countdown"></div>
        <button id="nextBtn" class="next-btn">🎯 Continue to Destination</button>
        
        <div class="footer">
            Powered by <a href="https://eas-x.com" target="_blank">EAS-X Technology</a> • 
            Based on <a href="https://trw.lat/install/userscript/u.raw.js" target="_blank">F.E.A.R. by Woozie</a>
        </div>
    </div>
</body>
</html>`;

        const script = document.createElement('script');
        script.textContent = 'window.ock=true;';
        document.head.appendChild(script);

        (function waitForOck() {
          if (unsafeWindow.ock === true) {
            const dest = unsafeWindow.dest || '';
            const countdownEl = document.getElementById('countdown');
            const btn = document.getElementById('nextBtn');

            function hasHash(url) {
              try {
                return (unsafeWindow.dest.includes("hash="))
              } catch {
                return false;
              }
            }

            if (hasHash(dest)) {
              countdownEl.style.color = '#ff4d4d';
              countdownEl.style.fontWeight = 'bold';
              let time = 8;
              countdownEl.textContent = `YOU HAVE EXACTLY ${time} SECONDS TO CLICK THE BUTTON BEFORE YOUR HASH EXPIRES`;
              const interval = setInterval(() => {
                time--;
                if (time > 0) {
                  countdownEl.textContent = `YOU HAVE EXACTLY ${time} SECONDS TO CLICK THE BUTTON BEFORE YOUR HASH EXPIRES`;
                } else {
                  countdownEl.textContent = `HASH EXPIRED! RESTARTING BYPASS PROCESS...`;
                  countdownEl.style.color = '#ff4d4d';
                  countdownEl.style.fontWeight = '';
                  btn.disabled = true;
                  clearInterval(interval);
                  setTimeout(() => {
                      // Get the truly original URL that started the bypass process
                      let retryUrl = null;
                      
                      // Priority 1: Early capture (most reliable)
                      if (unsafeWindow.easxEarlyCapture && !unsafeWindow.easxEarlyCapture.includes('favicon.ico')) {
                        retryUrl = unsafeWindow.easxEarlyCapture;
                      }
                      
                      // Priority 2: SessionStorage early capture
                      if (!retryUrl) {
                        try {
                          const earlyCapture = sessionStorage.getItem('easx_early_capture');
                          if (earlyCapture && !earlyCapture.includes('favicon.ico')) {
                            retryUrl = earlyCapture;
                          }
                        } catch (e) {}
                      }
                      
                      // Priority 3: SessionStorage original URL
                      if (!retryUrl) {
                        try {
                          const storedOriginal = sessionStorage.getItem('easx_original_url');
                          if (storedOriginal && !storedOriginal.includes('favicon.ico')) {
                            retryUrl = storedOriginal;
                          }
                        } catch (e) {}
                      }
                      
                      // Priority 4: unsafeWindow stored URLs
                      if (!retryUrl) {
                        if (unsafeWindow.easxOriginalUrl && !unsafeWindow.easxOriginalUrl.includes('favicon.ico')) {
                          retryUrl = unsafeWindow.easxOriginalUrl;
                        } else if (unsafeWindow.easxStoredOriginal && !unsafeWindow.easxStoredOriginal.includes('favicon.ico')) {
                          retryUrl = unsafeWindow.easxStoredOriginal;
                        }
                      }
                      
                      // Priority 5: Construct from hostname
                      if (!retryUrl) {
                        const hostname = location.hostname;
                        if (hostname.includes('linkvertise')) {
                          retryUrl = `https://${hostname}/`;
                        } else {
                          retryUrl = `https://${hostname}/`;
                        }
                      }
                      
                      // Final fallback
                      if (!retryUrl) {
                        retryUrl = 'https://linkvertise.com/';
                      }
                      
                      // Ensure we have a valid URL
                      try {
                        new URL(retryUrl);
                      } catch (e) {
                        retryUrl = 'https://linkvertise.com/';
                      }
                      
                      console.log('[EAS-X] Hash expired, retrying with URL:', retryUrl);
                      location.replace(retryUrl);
                  }, 3500);
                }
              }, 1000);
            } else {
              countdownEl.style.display = 'none';
            }

            btn.addEventListener('click', () => {
              if (!btn.disabled && unsafeWindow.dest) {
                window.location.href = unsafeWindow.dest;
              }
            });
          } else {
            setTimeout(waitForOck, 50);
          }
        })();

      } else {
        document.documentElement.innerHTML = `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EAS-X F.E.A.R. - Redirecting</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #1E88E5;
            --secondary: #2196F3;
            --background: #0F1419;
            --surface: #1A1F2E;
            --text: #E0E6ED;
            --text-secondary: #A0A6B0;
            --success: #4CAF50;
            --border: #2D3748;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--background);
            color: var(--text);
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            text-align: center;
        }

        .container {
            background: var(--surface);
            padding: 3rem 2.5rem;
            border-radius: 20px;
            border: 1px solid var(--border);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
        }

        .logo {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .status {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--success);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-weight: 600;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">🚀 EAS-X F.E.A.R.</div>
        <div class="status">✅ Redirecting to your destination...</div>
    </div>
</body>
</html>`;
        window.location.assign(decodeURIComponent(ref));
      }
      return;
    }

    if (location.href.includes(`${CONFIG.apiBase}/eas.lol`)) {
      // We're on the EAS.LOL wait page - let it handle the waiting
      notyf.information(`EAS.LOL processing your link...`);
      return;
    }

    if ((location.hostname === 'localhost' && (location.port === '13075' || location.port === '3000') && location.pathname === "/bypass") ||
        (location.hostname === 'usrnew.eas-x.com' && location.pathname === "/bypass")) {
      notyf.information("EAS-X is bypassing this link...");
      
      // Add retry button listener for bypass failures
      let retryAttempts = 0;
      const maxRetries = 3;
      
      const attemptBypass = async () => {
        const success = await findHref();
        if (success) {
          notyf.success(`Redirecting to destination...`);
          const redirect = p.get('url');
          const host = new URL(redirect).hostname;
          await sleep(2000);
          const bypassUrl = `https://${host}/favicon.ico?easxreferrer=${encodeURIComponent(success)}`;
          window.location.href = bypassUrl;
        } else {
          Log.e('Failed to find link');
          retryAttempts++;
          
          if (retryAttempts < maxRetries) {
            notyf.error(`Bypass failed. Attempt ${retryAttempts}/${maxRetries}. Setting up retry...`);
            
            // Set up retry button listener
            const setupRetryListener = () => {
              const retryBtn = findRetryButton();
              if (retryBtn) {
                Log.i('Retry button found, setting up listener');
                
                // Replace any existing onclick handlers
                retryBtn.onclick = null;
                
                retryBtn.addEventListener('click', async (e) => {
                  e.preventDefault();
                  e.stopPropagation();
                  
                  // Call the global retry function
                  unsafeWindow.easxRetry();
                });
                return true;
              }
              return false;
            };
            
            // Try to find retry button immediately
            if (!setupRetryListener()) {
              // If not found, set up observer to watch for it
              const observer = new MutationObserver((mutations) => {
                if (setupRetryListener()) {
                  observer.disconnect();
                }
              });
              observer.observe(document.body, {
                childList: true,
                subtree: true
              });
              
              // Stop observing after 30 seconds
              setTimeout(() => observer.disconnect(), 30000);
            }
            
          } else {
            notyf.error(`Bypass failed after ${maxRetries} attempts. Please check the URL and try again.`);
          }
        }
      };
      
      await attemptBypass();
      return;
    }

    if (location.href.includes(`${CONFIG.apiBase}/?url=`) && !location.href.includes('example')) {
      await sleep(1000);
      const info = document.getElementById('information');
      if (info) info.click();
      notyf.information(`EAS-X redirecting in ${CONFIG.wait}s...`);
      let timeLeft = CONFIG.wait;
      const h1 = document.querySelector('h1');
      if (h1) {
        h1.textContent = `EAS-X redirecting in ${timeLeft}s...`;
        h1.style.color = '#1E88E5';
        const countdown = setInterval(() => {
          timeLeft--;
          if (timeLeft <= 0) {
            clearInterval(countdown);
            h1.textContent = `Redirecting now...`;
          } else {
            h1.textContent = `EAS-X redirecting in ${timeLeft}s...`;
          }
        }, 1000);
      }
      await sleep(CONFIG.wait * 1000);
      const u = new URLSearchParams(location.search).get('url');
      // Redirect to EAS.LOL wait page with the specified wait time
      const protocol = isDevelopment ? 'http://' : 'https://';
      window.location.href = `${protocol}${CONFIG.site}${encodeURIComponent(u)}&wait=${CONFIG.wait}`;
      return;
    }
    
    if (CONFIG.SecureMode) {
        (function(){
          // Store original URL for retry functionality
          if (!unsafeWindow.easxOriginalUrl) {
            unsafeWindow.easxOriginalUrl = location.href;
            unsafeWindow.easxStoredOriginal = location.href;
            // Also store in sessionStorage for persistence
            sessionStorage.setItem('easx_original_url', location.href);
          }
          
          if(document.getElementById('easx-overlay')) return;
          const BYPASS=`${CONFIG.bypassPage}${encodeURIComponent(location.href)}`;
          const MAX_RETRIES=2;
          let retries=0, userChose=false;

          const makeStyle=()=>{
            const s=document.createElement('style');
            s.id='easx-overlay-style';
            s.textContent=`
              @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
              
              :root {
                --primary: #1E88E5;
                --primary-dark: #1565C0;
                --secondary: #2196F3;
                --background: #0F1419;
                --surface: #1A1F2E;
                --surface-light: #252B3F;
                --text: #E0E6ED;
                --text-secondary: #A0A6B0;
                --success: #4CAF50;
                --error: #F44336;
                --border: #2D3748;
              }
              
              #easx-overlay {
                position: fixed !important;
                inset: 0 !important;
                z-index: 2147483647 !important;
                display: flex !important;
                align-items: center !important;
                justify-content: center !important;
                pointer-events: auto !important;
                touch-action: none !important;
                background: rgba(15, 20, 25, 0.95) !important;
                backdrop-filter: blur(20px) !important;
                animation: fadeIn 0.3s ease-out !important;
              }
              
              @keyframes fadeIn {
                from { opacity: 0; }
                to { opacity: 1; }
              }
              
              @keyframes slideIn {
                from { 
                  opacity: 0; 
                  transform: translateY(-20px) scale(0.95); 
                }
                to { 
                  opacity: 1; 
                  transform: translateY(0) scale(1); 
                }
              }
              
              #easx-overlay * {
                box-sizing: border-box !important;
                pointer-events: auto !important;
                touch-action: manipulation !important;
              }
              
              #easx-panel {
                font-family: 'Inter', sans-serif !important;
                background: var(--surface) !important;
                color: var(--text) !important;
                padding: 2.5rem !important;
                border-radius: 20px !important;
                max-width: 500px !important;
                width: 90% !important;
                text-align: center !important;
                border: 1px solid var(--border) !important;
                box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4) !important;
                position: relative !important;
                overflow: hidden !important;
                animation: slideIn 0.4s ease-out !important;
              }
              
              #easx-panel::before {
                content: '' !important;
                position: absolute !important;
                top: 0 !important;
                left: 0 !important;
                right: 0 !important;
                height: 1px !important;
                background: linear-gradient(90deg, transparent, var(--primary), transparent) !important;
              }
              
              #easx-panel h2 {
                color: var(--text) !important;
                margin: 0 0 1rem !important;
                font-size: 1.75rem !important;
                font-weight: 700 !important;
                background: linear-gradient(135deg, var(--primary), var(--secondary)) !important;
                -webkit-background-clip: text !important;
                -webkit-text-fill-color: transparent !important;
                background-clip: text !important;
              }
              
              #easx-panel p {
                color: var(--text-secondary) !important;
                margin: 0 0 2rem !important;
                font-size: 1rem !important;
                line-height: 1.6 !important;
              }
              
              #easx-status {
                margin-bottom: 1.5rem !important;
                font-size: 0.875rem !important;
                font-weight: 600 !important;
                padding: 0.5rem 1rem !important;
                border-radius: 10px !important;
                background: var(--surface-light) !important;
                border: 1px solid var(--border) !important;
              }
              
              .easx-button-group {
                display: flex !important;
                gap: 1rem !important;
                justify-content: center !important;
                align-items: center !important;
                margin-bottom: 2rem !important;
              }
              
              #easx-panel button {
                padding: 0.875rem 1.5rem !important;
                border-radius: 10px !important;
                border: none !important;
                cursor: pointer !important;
                font-weight: 600 !important;
                font-size: 0.9rem !important;
                transition: all 0.3s ease !important;
                font-family: 'Inter', sans-serif !important;
                display: inline-flex !important;
                align-items: center !important;
                gap: 0.5rem !important;
                min-width: 140px !important;
                justify-content: center !important;
              }
              
              #easx-yes {
                background: linear-gradient(135deg, var(--primary), var(--secondary)) !important;
                color: white !important;
              }
              
              #easx-yes:hover {
                transform: translateY(-2px) !important;
                box-shadow: 0 10px 30px rgba(30, 136, 229, 0.3) !important;
              }
              
              #easx-no {
                background: var(--surface-light) !important;
                color: var(--text) !important;
                border: 1px solid var(--border) !important;
              }
              
              #easx-no:hover {
                background: var(--surface) !important;
                transform: translateY(-2px) !important;
              }
              
              .easx-footer {
                font-size: 0.75rem !important;
                color: var(--text-secondary) !important;
                margin-top: 1.5rem !important;
                opacity: 0.8 !important;
                line-height: 1.4 !important;
              }
              
              .easx-footer a {
                color: var(--primary) !important;
                text-decoration: none !important;
              }
              
              .easx-footer a:hover {
                text-decoration: underline !important;
              }
              
              @media (max-width: 480px) {
                #easx-panel {
                  padding: 2rem 1.5rem !important;
                  margin: 1rem !important;
                }
                
                .easx-button-group {
                  flex-direction: column !important;
                }
                
                #easx-panel button {
                  width: 100% !important;
                }
              }
            `;
            return s;
          };

          const create=()=>{
            if(document.getElementById('easx-overlay') || userChose) return;
            if(!document.getElementById('easx-overlay-style')) document.head.appendChild(makeStyle());

            const root=document.createElement('div');
            root.id='easx-overlay';
            root.setAttribute('role','dialog');
            root.setAttribute('aria-modal','true');
            Object.assign(root.style,{background:'rgba(0,0,0,.8)'});

            const panel=document.createElement('div');
            panel.id='easx-panel';
            panel.innerHTML=`
              <div id="easx-status">Checking EAS-X servers...</div>
              <h2>🚀 EAS-X F.E.A.R.</h2>
              <p>Do you want to bypass this ad-link using EAS-X professional bypassing technology?</p>
              <div class="easx-button-group">
                <button id="easx-yes" role="button" tabindex="0">
                  ✓ Yes — Start Professional Bypass
                </button>
                <button id="easx-no" role="button" tabindex="0">
                  ✗ No — Close
                </button>
              </div>
              <div class="easx-footer">
                Press Esc to close<br>
                Powered by <a href="https://eas-x.com" target="_blank">EAS-X Technology</a> • Based on <a href="https://trw.lat/install/userscript/u.raw.js" target="_blank">F.E.A.R. by Woozie</a>
              </div>
            `;

            root.appendChild(panel);
            document.body.appendChild(root);

            const prev=document.documentElement.style.overflow;
            document.documentElement.style.overflow='hidden';

            const cleanup=()=>{
              userChose=true;
              document.documentElement.style.overflow=prev||'';
              document.getElementById('easx-overlay')?.remove();
              mutation.disconnect();
            };

            const stopAndAct=(ev,fn)=>{try{ev.stopPropagation();}catch(e){}try{ev.preventDefault();}catch(e){}fn();};

            const yes=document.getElementById('easx-yes');
            const no=document.getElementById('easx-no');

            const onYes=ev=>stopAndAct(ev,()=>{
              cleanup(); 
              // Wait a bit before redirecting to ensure user sees the action
              setTimeout(() => {
                // Redirect to EAS.LOL wait page instead of direct bypass
                const protocol = isDevelopment ? 'http://' : 'https://';
                const easLolUrl = `${protocol}${CONFIG.site}${encodeURIComponent(location.href)}&wait=${CONFIG.wait}`;
                location.assign(easLolUrl);
              }, 500);
            });
            const onNo=ev=>stopAndAct(ev,cleanup);

            yes.addEventListener('pointerdown',onYes,{capture:true});
            yes.addEventListener('click',onYes,{capture:true});
            yes.addEventListener('touchstart',onYes,{capture:true,passive:false});

            no.addEventListener('pointerdown',onNo,{capture:true});
            no.addEventListener('click',onNo,{capture:true});
            no.addEventListener('touchstart',onNo,{capture:true,passive:false});

            const onKey=e=>{
              if(e.key==='Escape') cleanup();
              if(e.key==='Enter'||e.key===' '){
                const active=document.activeElement;
                if(active===yes) onYes(e);
                if(active===no) onNo(e);
              }
            };
            document.addEventListener('keydown',onKey,{capture:true});

            const observer=new MutationObserver(()=>{
              if(!document.getElementById('easx-overlay')) document.removeEventListener('keydown',onKey,{capture:true}); observer.disconnect();
            });
            observer.observe(document.documentElement,{childList:true,subtree:true});

            const statusEl=document.getElementById('easx-status');
            fetch(`${CONFIG.apiBase}/health`,{cache:'no-cache',redirect:'manual'})
              .then(r=>r.ok?r.json():Promise.reject(r))
              .then(json=>{
                if(json.status==='success'){statusEl.textContent='✅ EAS-X Professional Servers Online'; statusEl.style.color='#4CAF50';}
                else{statusEl.textContent=`⚠️ Server status: ${json.status}`; statusEl.style.color='#FFC107';}
              })
              .catch(()=>{statusEl.textContent='❌ EAS-X Servers Offline'; statusEl.style.color='#f44336';});
          };

          // Wait before creating overlay to ensure user sees the page first
          setTimeout(create, 1000);

          const mutation=new MutationObserver(()=>{
            if(!userChose && !document.getElementById('easx-overlay') && retries<MAX_RETRIES){
              retries++;
              setTimeout(create,300);
            } else if(retries>=MAX_RETRIES) mutation.disconnect();
          });
          mutation.observe(document.documentElement,{childList:true,subtree:true});
        })();
    } else{
        // Store original URL for retry functionality
        if (!unsafeWindow.easxOriginalUrl) {
          unsafeWindow.easxOriginalUrl = location.href;
          unsafeWindow.easxStoredOriginal = location.href;
          sessionStorage.setItem('easx_original_url', location.href);
        }
        
        document.documentElement.innerHTML = `<h2>EAS-X F.E.A.R. - Advanced Bypass System</h2>`;
        const bypassUrl = `${CONFIG.apiBase}/eas.lol?url=${encodeURIComponent(location.href)}&wait=${CONFIG.wait}`;
        window.location.assign(bypassUrl);
    }
  }

  (async () => {
    try {
      Log.i('EAS-X F.E.A.R. initializing...');
      await main();
    } catch (e) {
      Log.e(`Init failed: ${e.message}`);
      notyf.error('EAS-X Error occurred. Please contact support.');
    }
  })();
})();
